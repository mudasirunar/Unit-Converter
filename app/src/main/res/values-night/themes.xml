<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Default App Theme (Inherits light style definitions or sets defaults) -->
    <style name="Theme.UnitConverter" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash Screen Theme (Dark Mode) -->
    <style name="Theme.UnitConverter.Splash" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background_dark</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>
</resources>
